apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodbb13-pv
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data  
  # persistentVolumeReclaimPolicy: Retain